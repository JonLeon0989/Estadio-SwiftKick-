// reducers/appointmentsReducer.js
const initialState = []; // O el estado inicial que desees

const appointmentsReducer = (state = initialState, action) => {
  switch (action.type) {
    // Define los diferentes casos para actualizar el estado de las citas
    default:
      return state;
  }
};

export default appointmentsReducer;